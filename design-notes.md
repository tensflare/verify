Our typographic family is DM Sans + DM Mono. 

- No document metaphor. The icon is pure geometry — circles, lines, brackets. It reads as precision instrumentation rather than legal paperwork.
- Colors: Forest green (#3B6D11) on white, stepping up to #97C459 on dark. Single accent, nothing else.
- Mono does more work here. The citation text inside the reticle is the logo's most distinctive detail — it means the monospace font isn't just for labels, it's load-bearing identity.
- The corner brackets are the repeatable motif — they can show up on cards, badges, code blocks, verified status indicators anywhere in the UI.
- Tone: Forensic, not legal. Closer to a dev tool than a law firm. That's intentional — it's API-first software that happens to serve lawyers.